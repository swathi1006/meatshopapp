import 'package:flutter/material.dart';

class SplashPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: Center(
        child: Text("Meat at your doorstep",
        style: TextStyle(fontWeight: FontWeight.bold),),
      ),
    );
  }
}
